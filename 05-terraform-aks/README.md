# Task 5 – Terraform AKS

**For reviewers:** In this folder I have kept `aks-terraform-script.tf` (resource group, Log Analytics workspace, AKS with oms_agent), `variables.tf`, and `terraform.tfvars.example`.

**Usage:** `terraform.tfvars`, set the values (resource group name, cluster name, location, etc.). Run: `terraform init`, `terraform plan`, `terraform apply`.

**Deliverables:** I have left `output-logs/` for `terraform plan` and `terraform apply` output (e.g. apply-output.txt).

# Terraform output logs (Task 5)

**For reviewers:** I have left this folder for `terraform plan` and `terraform apply` output. After running `terraform init`, `terraform plan`, `terraform apply`: **plan-output.txt** – Output of `terraform plan`. **apply-output.txt** – Output of `terraform apply` (resource creation and outputs).

# Task 8 – Kubernetes Deployment

**For reviewers:** In this folder I have kept `deployment.yaml` – Deployment and Service for the sample app, with readiness and liveness probes (HTTP GET on `/`).

**Usage:** `kubectl apply -f deployment.yaml` (after connecting to my AKS cluster).


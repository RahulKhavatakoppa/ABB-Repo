# Task 12 – Pipeline Security

**For reviewers:** In this folder I have kept `security-pipeline.yml` – the GitHub Actions workflow: build and test, then dependency vulnerability scan (npm audit).

**To run on GitHub:** Copy `security-pipeline.yml` to `.github/workflows/security.yml`.
